- name: <insert-name>
  image:
    dev:
    prod:
  url:
    dev:
    prod:

  auth:
  enabled: true
  secrets: false
  service_name:
  traefik_port:

  commands:
  commands_https:
  environment:
  port_map:
  volumes:

  disable_restart: false
  path_prefix:
